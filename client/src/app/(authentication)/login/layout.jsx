import { Box } from "@mui/material";
import React from "react";

export const metadata = {
  title: "BrainStorming - Login",
  description: "Log in To BrainStorming",
};

const LoginLayout = ({ children }) => {
  return children;
};

export default LoginLayout;
