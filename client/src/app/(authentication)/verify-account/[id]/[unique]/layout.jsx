import React from "react";

export const metadata = {
  title: "BrainStorming - Verify Account",
  description: "Verify Your Account In BrainStorming",
};

const VerifyLayout = ({ children }) => {
  return children;
};

export default VerifyLayout;
